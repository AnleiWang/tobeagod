#include <stdio.h>
#include <assert.h>

#define MAX_LEN 10000

int q[MAX_LEN], head, tail;

void init() {
    head = 0;
    tail = -1;
}

// 请在下面实现插入函数 push
void push(int element) {
    assert(tail + 1 < length);
}

int main() {
    init();
    return 0;
}